import { AppointmentClient } from "./AppointmentPage";

export default function AppointmentPage() {
  return (
    <main className="flex min-h-screen w-full flex-col items-center bg-zinc-50 font-sans dark:bg-black">
      <AppointmentClient />
    </main>
  );
}
